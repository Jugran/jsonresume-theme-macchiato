{{#if resume.education.length}}
<div class="container education-container">
    {{> title value="Education"}} {{#each resume.education}}
    <section class="item">
        {{#if studyType}}
            {{#if area}}
                {{> section-header name=(concat this.studyType  ' In ' this.area) }}
            {{else}}
                {{> section-header name=(concat this.studyType) }}
            {{/if}}
        {{/if}}

        <div class="flex-container">
            {{#if institution}}
                <h5> {{institution}}</h5>
            {{/if}}

            {{#if location}}
                <h6 class="location">{{location}}</h6>
            {{/if}}
        </div>

        {{#if gpa}}
        <h6>{{gpa}}</h6>
        {{/if}} {{#if specialization}}
        <h4 class="specialization">{{specialization}}</h4>
        {{/if}} {{#if courses.length}}
        <ul class="two-column">
            {{#each courses}}
            <li>{{.}}</li>
            {{/each}}
        </ul>
        {{/if}}
    </section>
    {{/each}}
</div>
{{/if}}